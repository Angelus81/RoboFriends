import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './containers/App';
// import registerServiceWorker from './registerServiceWorker';
import 'tachyons';


ReactDOM.render(<App />, document.getElementById('root'));
// registerserviceWorker();

/// you want your app to work offline and load faster, you can change
// unregister() to register() below. Note this comes with some pitfalls.
// Learn more about service workers: https://bit.ly/CRA-PWA


// ********Compiled message*******
// 'robots' is defined but never used  no-unused-vars
// to solve this robots is not a default import so it has
// to go into { robots } (destructure it).

// If another variable is added into card.js export const cats
// then in index.js it will be import { robots, cats }

